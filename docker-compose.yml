services:
  bot:
    build: .
    image: quantumforge-rag-bot:latest
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    volumes:
      - huggingface_cache:/root/.cache/huggingface
    stdin_open: true
    tty: true

volumes:
  huggingface_cache:
